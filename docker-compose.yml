services:
  job-app:
    container_name: job-app
    build: .
    restart: unless-stopped
    ports:
      - "${JOB_APP_BIND:-127.0.0.1}:${JOB_APP_PORT:-8080}:8080"
    volumes:
      - ./data:/app/data
      - ./config:/app/config
